services:
  dev:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile.dev
    volumes:
      - ..:/workspace:cached
      - uv_cache:/root/.cache/uv
    env_file:
      - path: ../.env
        required: false

volumes:
  uv_cache:

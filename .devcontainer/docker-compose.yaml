services:
  dev:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile.dev
    command: sleep infinity
    volumes:
      - ..:/workspace:cached
      - uv_cache:/root/.cache/uv
    env_file:
      - path: ../.env
        required: false

volumes:
  uv_cache:
